name : CICD pipeline
on:
    push:
        branches : ["main"]
    pull_request:
        branches : ["main"]
    
jobs:
    build:
         runs-on: ubuntu-latest
         steps:
            - name : run server
              run: echo i am yash

         
            - name : Install dependies
              run : |
                python -m pip install --upgrade pip
              

    Test:
        runs-on : ubuntu-latest
        steps:
           - name : yash created server
             run : echo yash built server
        


        
