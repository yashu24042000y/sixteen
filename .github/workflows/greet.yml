name : yash cicd pipeline

on:
    push:
        branches : ["main"]
    pull_request:
        branches : ["main"]

jobs :
    build :
        runs-on : ubuntu-latest

        steps: 
            - name : run the dependencies
              run : echo dependencies are running...

            - name : running the testapp
              run : test app is running...

            - name : checkout the code 
              uses : actions/checkout@v4
        


        
